# Backup-Hinweis

Vor dem Neuaufbau wurde der bisherige Stand des Repositorys unverändert auf folgendem Zweig gesichert:

- Branch: `backup/vor-neuaufbau-2026-08-09`
- Ausgangscommit: `61530dfc5e0063dc0ed7003f0f59cae2947199f0`

Der neue `main`-Stand wird ausschließlich auf einer neu erzeugten Root-Dateistruktur basieren. Die alte Historie bleibt über Git und den Backupzweig rückholbar.
